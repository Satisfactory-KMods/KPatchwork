[project]
name = "kpatchwork-ci"
version = "0.1.0"
description = "KPatchwork CI and KDataForge pack tooling"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0,<7",
]

[tool.uv]
package = false
